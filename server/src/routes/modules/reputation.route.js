/**
 * create route Reputation for project
 */
const express = require('express');

const reputation = require('../../controllers/reputation.controller'); 

const router = express.Router();
/* GET Reputation listing. */
router.route('/')
    
module.exports =router;

